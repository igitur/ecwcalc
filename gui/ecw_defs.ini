sinD(x) = sin(rad(x))
cosD(x) = cos(rad(x))
comb(n,k) = fact(n)/fact(k)/fact(n-k)
inf = 1e4932
if(flag, x1, x2) = x1*(flag<>0) + x2*(flag=0)
limit(x, x_min, x_max) = if(x<x_min, x_min, if(x>x_max, x_max, x))
