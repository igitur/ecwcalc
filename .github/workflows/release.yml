name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.suffix }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            suffix: linux-x64
            exe: ''
            battery: true
          - os: windows-latest
            suffix: windows-x64
            exe: '.exe'
            battery: true
          # macos-latest runners are arm64: FPC's 80-bit Extended falls back
          # to Double there, so the byte-exact differential battery is
          # x86_64-only (Ubuntu + Windows); macOS gets build + smoke tests.
          - os: macos-latest
            suffix: macos-arm64
            exe: ''
            battery: false

    steps:
      - uses: actions/checkout@v4

      # ---------------- toolchains ----------------
      - name: Install FPC + Lazarus (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y fpc lazarus \
            libxkbcommon-dev libwayland-dev libx11-dev \
            libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxcb1-dev \
            libgl1-mesa-dev libegl1-mesa-dev

      - name: Install FPC + Lazarus (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install fpc
          # homebrew cask 'lazarus' is deprecated (disabled 2026-01-27) and its
          # fpc-src pkg is incompatible with current macOS, so pull the
          # official aarch64 zip (IDE + lazbuild) directly instead.
          curl -L --retry 3 -o /tmp/lazarus.zip \
            "https://download.lazarus-ide.org/Lazarus%20macOS%20aarch64/Lazarus%204.8/lazarus-darwin-aarch64-4.8.zip"
          mkdir -p /tmp/lazarus && unzip -q /tmp/lazarus.zip -d /tmp/lazarus
          xattr -cr /tmp/lazarus 2>/dev/null || true
          LAZBUILD=$(find /tmp/lazarus -name lazbuild -type f | head -1)
          echo "lazbuild: $LAZBUILD"
          test -n "$LAZBUILD" && echo "$(dirname "$LAZBUILD")" >> "$GITHUB_PATH"
          which fpc || true

      - name: Install FPC + Lazarus (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # choco 'lazarus' downloads from SourceForge (flaky, EOF mid-stream);
          # grab the official mirror exe directly and silent-install it.
          curl -L --retry 3 -o /tmp/lazarus.exe \
            "https://download.lazarus-ide.org/Lazarus%20Windows%2064%20bits/Lazarus%203.6/lazarus-3.6-fpc-3.2.2-win64.exe"
          /tmp/lazarus.exe /VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-
          echo "C:\\lazarus" >> "$GITHUB_PATH"
          FPCVER=$(ls /c/lazarus/fpc | head -1)
          echo "C:\\lazarus\\fpc\\${FPCVER}\\bin\\x86_64-win64" >> "$GITHUB_PATH"
          which fpc lazbuild || true

      - uses: dtolnay/rust-toolchain@stable

      # ---------------- build: FPC CLI ----------------
      - name: Build FPC CLI
        run: fpc -O3 ecw.pas

      - name: Smoke-test CLI
        shell: bash
        run: |
          test "$(./ecw${{ matrix.exe }} '2+3*4')" = "14"
          test "$(./ecw${{ matrix.exe }} '2**1000')" = "1.07150860718627E+0301"
          test "$(./ecw${{ matrix.exe }} '0xFFFFFFFF')" = "-1"
          test "$(./ecw${{ matrix.exe }} --unsigned '0xFFFFFFFF')" = "4294967295"

      # ---------------- build: Lazarus GUI ----------------
      - name: Build Lazarus GUI
        run: lazbuild gui/ecwcalc.lpi

      # ---------------- build: vizia (Rust) ----------------
      - name: Build vizia GUI
        working-directory: vizia
        run: cargo build --release

      - name: Rust unit tests
        working-directory: vizia
        run: cargo test --lib

      - name: Differential battery vs FPC CLI (x86_64 only)
        if: matrix.battery
        working-directory: vizia
        env:
          ECW_FPC_BIN: ${{ github.workspace }}/ecw${{ matrix.exe }}
        run: cargo test --test battery

      # ---------------- stage + upload ----------------
      - name: Stage binaries
        shell: bash
        run: |
          mkdir -p dist
          cp ecw${{ matrix.exe }}        dist/ecw-${{ matrix.suffix }}${{ matrix.exe }}
          cp gui/ecwcalc${{ matrix.exe }} dist/ecwcalc-${{ matrix.suffix }}${{ matrix.exe }}
          cp vizia/target/release/ecw-vizia${{ matrix.exe }} dist/ecw-vizia-${{ matrix.suffix }}${{ matrix.exe }}

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.suffix }}
          path: dist/
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List staged binaries
        run: ls -la dist/

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
